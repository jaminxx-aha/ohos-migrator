import process from '@ohos.process';

process.exit(0);
process.getEnvironmentVar('');
process.getSystemConfig(0);
process.getThreadPriority(0);
process.getUidForName('');
process.isAppUid(0);
process.kill(0, 0);
