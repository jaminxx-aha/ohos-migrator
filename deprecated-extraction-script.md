---
name: deprecated-extraction-script
description: ohos-migrator 项目结构与废弃函数迁移规则集（ts-morph 索引 + 扫描 + 重写管线）
metadata:
  node_type: memory
  type: project
  originSessionId: 34643758-d30e-4893-9711-e6f7576f09ce
  modified: 2026-08-05T16:52:09.129Z
---

ohos-migrator 项目位于 `/Users/aha/code/ohos-migrator`，是鸿蒙废弃函数迁移工具（非早期的独立 `extract_deprecated_ts.js`，已废弃）。3 阶段管线：index(ts-morph) → scan(正则/offset tolerant) → rewrite(自底向上 offset 拼接)。

**索引**：`src/indexer/sdk-indexer.ts` 递归收集 `.d.ts/.d.ets`，跳过 `@internal/`；嵌套文件经 re-export 追溯归属顶层 kit；输出 `DeprecationMap`（entries + kitIndex + exportIndex），缓存于 `.harmony-deprecate/deprecation-map.<apiVersion>.json`（API 24 = 4713 条，其中带 @useinstead 3891 条）。

**规则种类（RuleKind）与自动可替换性**：
- `rewrite-import` —— kit 整体迁移（kitIndex[oldKit].newKit）改 import specifier；或**跨 kit 命名导入 drop-in**：named-import 子句中所有 binding 都同名迁移到同一目标 kit 时改 specifier 保名（per-clause 安全，混合目标/含无替代项的子句不动）。由 `crossKitDropin` 索引驱动。**默认导出迁移**（`export default class X` 跨 kit，indexer 用 `hasDefaultModifier` 检测，键为 `${kit}\0default`）也走 drop-in：`import X from '@old'`→`import X from '@new'`（仅改 specifier，本地 binding 是 default 导出，保名，调用点不动）。scanner 的 `scanProjectCrossKitDropin` 统一处理 default+named binding（per-clause：所有 binding 含 default 须同目标 kit，否则整子句不动；default binding 不可别名，只改 specifier）。
- `rename-member` —— 同 kit（或 kit-move 对齐）且**替换链与废弃链等长**的改名：把 `binding.<old chain>` 整条拼成 `binding.<new chain>`。覆盖 leaf-only、container/mid-segment、container+leaf 三类（如 `rpc.MessageParcel.create`→`rpc.MessageSequence.create`、`media.MediaErrorCode.MSERR_OK`→`media.AVErrorCode.AVERR_OK`）。链长不等→manual（调用形状变了）。亦用于**跨 kit 等长成员迁移**（kit 未整体迁移、repl.kit 是经 indexer 验证的真实 kit 且 repl 链首段为其顶层导出，entry 标 `crossKitMemberDropin`）：scanner 注入 `import * as <新binding> from '<repl.kit>'` 并把调用点 receiver 重绑到新 binding、整条 repl 链拼出。覆盖 1-seg 单 leaf（如 `particleAbility.startBackgroundRunning`→`backgroundTaskManager.startBackgroundRunning`）与**多段路径保留迁移**（2-seg，链全等、仅 kit 变，如 `bluetoothManager.A2dpSourceProfile.connect`→`bluetooth.a2dp.A2dpSourceProfile.connect`、`bluetoothManager.HandsFreeAudioGatewayProfile.disconnect`→`bluetooth.hfp.HandsFreeAudioGatewayProfile.disconnect`），以及 container/leaf 单段改名的跨 kit 等长迁移（如 `SppOption.uuid`→`SppOptions.uuid`）。还覆盖**跨 kit 2→1 命名空间 flatten**（`crossKitMemberDropin`，round ⑱）：被丢容器须为 ownKit 的 NAMESPACE（如 `bluetoothManager.BLE.startBLEScan`→`bluetooth.ble.startBLEScan`，`bm.BLE.startBLEScan()` 合法值位访问→`ble.startBLEScan()`）；interface/class/enum 容器 inert 或语义重定向，拒（留 manual）。repl 各段须为干净标识符；@useinstead 的 `name:value` 事件提示碎片（如 `connection.on#event:bluetoothDeviceFind` 解析出 `["on","event:bluetoothDeviceFind"]`）由 `toReplSymbol` 滤除冒片段后还原为真链 `["on"]`（见下文 round ⑥）。**歧义门**：同一废弃符号 `(dep.kit, dep.members)` 映射到 >1 个 repl.kit 时（如 `bluetoothManager.on` 按事件分到 connection/access/socket 三 kit），scanner 只匹配符号无法读事件名串选目标，自动重绑会乱选→indexer `resolveCrossKitMemberAmbiguity` 把这类 entry 的 `crossKitMemberDropin` 撤标留 manual；唯一目标的符号保持 drop-in。
- `rename-export` —— 同 kit 导出/类改名，用安全别名保本地 binding：`{By}`→`{On as By}`，body 不改。exportIndex 由 indexer 构建。**跨 kit 不同名导出**（`crossKitRenameExport: ${oldKit}\0${old} -> ${newKit}\0${new}`）也走别名：specifier 改到新 kit 且 binding 别名为 `{new as old}`（如 `@ohos.fileio.fstat`→`@ohos.file.fs/stat`，`import { fstat }`→`import { stat as fstat } from '@ohos.file.fs'`，调用点不变）。per-clause：子句所有 binding 须迁到同一目标 kit（同名 dropin 或异名 rename 混合均可），否则整子句不动。由 `scanProjectCrossKitDropin` 统一处理（同名只改 specifier、异名额外发 rename-export finding 别名 binding）。
- `override` —— UIContext `get<Head>()` recipe 与 `@ohos.window` WindowStage/Window recipe（recipe 表见 `src/rewriter/overrides.ts`，跨 kit 才触发）。
- `inject-import` —— 跨 kit 等长成员迁移的"注入新 import"部分（与上面 rename-member 的 rebind finding 配对）：scanner 每 file 合并一次，在 import 块末尾零长插入新 import 行；rewriter 底向上 offset 拼接处理纯插入。命名冲突时自动加数字后缀（`a2dp`→`a2dp2`）；目标 kit 已被该 file 导入则复用既有 binding 不注入。一 file 内多个不同目标 kit（如 bluetooth.a2dp + bluetooth.hfp）分别注入各自 import。**零长 inject 与 rebind 同 offset 修正**：当废弃调用点紧跟 import 块下一行首时，inject 锚点（最后 import 行后换行处）与 rebind 匹配 offset 重合，rewriter 的 `dedupeOverlapping` 原把零长 inject 误判为"与 rebind 重叠"而丢弃（漏注入、`a2dp` 变未定义）。修正：零长插入（start===end）不占字符，永不因重叠丢弃；且底向上排序 tie-break `b.end-a.end`，使同 offset 处先应用 replacement（消费 [X,X+len]）再应用 insertion，避免顺序倒置错切文本。
- `manual` —— 其余。

**索引 kit 范围**：顶层 `@ohos.*` 与 `@system.*`（API 9 前遗留系统 kit，同为可导入顶层模块）。`@?` 合成 kit 仅用于无 importer/re-exporter 的真孤儿嵌套文件（已从 495 降到 23）。`@useinstead` kit 前缀大小写不敏感解析（kitLookup）。re-export 追溯为 fixpoint worklist：处理顶层 kit 的 `import` 与 `export {X} from`/`export *`，并递归处理被归属的嵌套文件的再导出（解析 `@ohos.bundle → bundle/bundleInfo → bundle/hapModuleInfo` 这类二级嵌套）。

**嵌套匹配去重**：SDK 常同时废弃类与成员（`rpc.MessageParcel` 与 `rpc.MessageParcel.create`），二者正则命中同一调用点。scanner 每 span 只保留最长 finding，rewriter 丢弃重叠 edit，避免拼接两次导致文本损坏（曾产出 `createte`）。

**kit-move 对齐（重要）**：跨 kit 成员若 `kitMove(dep.kit)===repl.kit`，则 rewrite-import 已重指 binding，视为"等效同 kit"：链不变→抑制（去除 ~1225 误报 manual）；等长链变→rename-member（如 bluetooth.getProfileConnState→getProfileConnectionState）。

**大小写不敏感 kit 解析**：useinstead-parser 用 lowercased→real kitLookup，修正 SDK 中 `ohos.uitest.Component` 这类错大小写限定符，使 UiComponent→Component / UiDriver→Driver 进入 exportIndex。

**分类现状**（API 24 真实 map 4713 条，带 @useinstead 3891 条；2026-08-13 round ⑳ faithful 分类器复算，口径=镜像 scanner/rewriter 实际分类）：**auto/covered 3441 条（72.8%）；manual 1272 条（27.2%：no_replacement 823、cross-kit/chain-length-mismatch member 439、export-other 10）**。注：原 doc headline 的 3502/74.3% 来自已删 classify 脚本的 round ⑮ base 3433 + 可靠 +69 flag delta；faithful 复算得 base 3372，原 base 高估 61——根因是删除分类器把 aligned-kit-move 但**成员链已变**（删成员/语义变化，如 `AbilityLifecycleState.UNINITIALIZED`→`AbilityLifecycleState` 删枚举项、`StoreConfig.name`→`StoreConfig` 删成员、`Constants`→`Constants.MAX_KEY_LENGTH` 枚举→具体项）的条目误计 auto（"kit 迁了→auto" 忽略链变）；faithful 版要求 no-op/rename-member 须链全等/等长，这些正确留 manual。faithful 分类器 `scripts/classify-faithful.mjs` 为可复现 source of truth。六轮冗余抑制/扩展已落地：①自指 no-op（sameKit 且链全等，原 90 条）归 rewrite-import suppressed 不报；②已被 crossKitDropin specifier 改写覆盖、链相同的成员（原 11 条：Configuration.language/.colorMode + 9 个 Want 成员）在 scanProjectMembers 抑制；**round ⑤（2026-08-10）**：将此抑制推广到**实例扫描器**（scanProjectInstanceMembers + tsc 变体）——给 `instanceFinding` 加 `ContainerDropinResolver`，当成员的容器经 crossKitDropin 跨 kit 同名迁移、且成员 repl.kit===dropin 目标、链全等时返回 null 抑制。消除 `cfg.language`/`w.deviceId` 之类"实例访问被报 manual、而容器 import 已被 rewrite-import 改写覆盖"的冗余误报（真实 SDK 实测：单文件 8 finding→2，6 条冗余 manual 消失，0 manual 遗留）。per-entry 计数不变（11 条 round ② 已归 auto），是 scanner 输出保真度修正。③**跨 kit 单 leaf 成员迁移**（indexer 用 `kitTopLevelExports` 验证 repl leaf 为目标 kit 顶层导出后打 `crossKitMemberDropin`，193 条，如 particleAbility→backgroundTaskManager）：scanner 注入新 import + 重绑 receiver，由 manual cross-kit 转入 auto。④**跨 kit 等长多段成员迁移**（`crossKitMemberDropin` 由 1-seg 推广到 N→N 等长；验证 repl 链首段为目标 kit 顶层导出、各段须为干净标识符拒 `event:BLEDeviceFind` 解析碎片，329 条，如 bluetoothManager.A2dpSourceProfile.connect→bluetooth.a2dp.A2dpSourceProfile.connect、bluetoothManager.HandsFreeAudioGatewayProfile.disconnect→bluetooth.hfp...；其中 317 条链全等零改名、12 条 container/leaf 单段改名、2 条解析碎片留 manual）：同样注入新 import + 整条 repl 链重绑 receiver。**round ⑥（2026-08-10）**：**@useinstead 事件提示碎片滤除 + 歧义门 + rewriter 同 offset 注入修正**。`toReplSymbol` 滤除 repl.members 中含冒号的段（JS 标识符不含冒号，`:` 段必是 `#event:NAME` 解析碎片，如 `connection.on#event:bluetoothDeviceFind`→`["on","event:bluetoothDeviceFind"]`→还原 `["on"]`），使 56 条原链长不等（碎片多一段）的 cross-kit 条目还原为等长路径保留迁移、经验证后打 `crossKitMemberDropin` 转入 auto；其中 10 条 1-seg `bluetoothManager.on/off` 映射到 connection/access/socket 三 kit（歧义），`resolveCrossKitMemberAmbiguity` 后置扫描按 `(dep.kit, dep.members)` 聚合 repl.kit，>1 目标则撤标留 manual（scanner 只匹配符号无法读事件名串选目标，自动重绑会乱选）。净增 auto +46（522→568 flagged，56 滤除−10 歧义）、manual cross-kit bucket +10（碎片→等长但歧义）、chain-length-mismatch −56（碎片多一段→还原等长）。另修正 rewriter `dedupeOverlapping` 在 inject 锚点与 rebind 同 offset（调用点紧贴 import 块下一行首）时丢弃零长 inject 的 bug（零长插入不占字符不因重叠丢弃 + 底向上 tie-break 保 replacement 先于 insertion）。**round ⑦（2026-08-10）**：**模块级声明收集修正 + 跨 kit 成员 drop-in 与同名导出 drop-in 冲突门**。indexer 原 `collectKitTopLevelExports` 只进入首个 `declare namespace` body 收集顶层导出名，漏掉与 namespace 平级的模块级 `declare function`/`const`/`class`/`enum`（如 `@ohos.file.fs.d.ts` 同时有 `declare namespace fileIo` 与模块级 `declare function moveFile`/`listFile`/`access`，旧代码只收 `OpenMode` 漏掉这三个→无法验证 `system.file.move→file.fs.moveFile`）。修正：遍历 `sourceFile.getStatements()`，namespace 声明进其 `ModuleBlock` 收集、非 namespace 顶层语句直接收名（含 `declare function`、`VariableStatement` 取首声明名）；使 12 条系统 file.* 同名模块级 drop-in 正确验证、打 `crossKitMemberDropin` 转入 auto。**冲突门**：`@ohos.fileio` 的模块级 `read` 是同名导出 drop-in（`fileio.read→file.fs.read`，exportIndex/crossKitDropin 改 specifier 保名，是符号 `fileio.read` 的权威替代），但同名 1-seg 成员 `fileio.read→file.fs.listFile`（语义重定向、leaf 变）若也打 `crossKitMemberDropin` 标会令 namespace 用法 `fileio.read(...)` 错重绑到 `listFile`。新增 `resolveCrossKitMemberExportConflict`：1-seg 成员 entry 的 leaf 若存在同名 crossKitDropin 且 repl.kit===dropin 目标、repl leaf 与 leaf 不同→撤标留 manual（语义重定向非 drop-in）；2-seg 路径保留迁移不受影响。净增 auto +19（`crossKitMemberDropin` 568→588，+20 新标其中 1 条原已属 auto 故净 +19），manual 1469→1450。**round ⑧（2026-08-10）**：**嵌套文件导出索引门放宽**。indexer 原 export-rename / crossKitDropin / crossKitRenameExport 构建块以 `isTopLevel(filePath)` 守卫，只处理 kit 顶层文件（`@ohos.*.d.ts`/`@system.*.d.ts`）内的 memberless 导出声明，漏掉 kit 通过 re-export 聚合的嵌套文件导出（如 `@ohos.arkui.modifier` 经 `export { NavigatorModifier } from './arkui/NavigatorModifier'` 聚合的 `NavigatorModifier`、`@ohos.ability.featureAbility` 经 `export { ElementName } from './bundle/elementName'` 聚合的 `ElementName`/`CustomizeData`/`ModuleInfo`、`@ohos.settings` 聚合的 `ResultSet`）。这些嵌套文件的 memberless 导出声明经 `resolveOwnKit` re-export 追溯已归属到真实可导入 kit（非 `@?` 孤儿），是 kit 顶层导出，应享受与顶层文件声明相同的 exportIndex/crossKitDropin/crossKitRenameExport 处理。修正：守卫由 `isTopLevel(filePath)` 改为 `!ownKit.startsWith("@?")`（真实 kit，含顶层文件 + re-export 归属的嵌套文件），`!isNamespaceLevel` 仍排除 kit namespace 内的子命名空间（如 bluetoothManager 内的 `BLE`）。净增 auto +9（export-level manual 14→5：4 个 arkui.modifier 同名导出改名→exportIndex、1 个 arkui.node 改名→exportIndex、settings/ResultSet + featureAbility/ElementName 跨 kit 同名→crossKitDropin、featureAbility/CustomizeData + ModuleInfo 跨 kit 异名→crossKitRenameExport），manual 1450→1441。**round ⑨（2026-08-11）**：**非对称 1→2 容器 drop-in 抑制**。round ②/⑤ 的容器 drop-in 抑制只认**对称**形状（容器在 `dep.exportName`、repl 链==dep 链、容器随 crossKitDropin 同名迁移、repl.kit===dropin）。但真实 SDK 另有一批**非对称**形状：容器（类/接口，如 `fileio.Stat`/`ResultSet`/`Stream`/`Watcher`）在 `dep.exportName`、leaf 在 `dep.members`（1 段），repl **重述容器**为 `repl.members=[容器, leaf]`（如 `fileio.Stat.ino`→`file.fs.Stat.ino`，dep.exportName=Stat、dep.members=[ino]、repl.members=[Stat,ino]）。容器经同名 crossKitDropin 整体迁移后，实例访问 `s.ino` 在重指后的 binding 上自然解析，无需成员级拼改；但旧抑制的等长门（`rm.length===dm.length`）拒非对称→实例扫描器对 `s.ino` 仍报冗余 "wiring changes" manual。新增共享 `memberCoveredByContainerDropin`：要求 `repl.kit===dropin`（成员随容器迁移）后，对称（链全等）**或**非对称（`rm.length===dm.length+1 && rm[0]===exportName && dm.every((m,i)=>m===rm[i+1])`）皆抑制；binding 扫描器（`scanProjectMembers`）与实例扫描器（`instanceFinding`）两处调用点统一替换。binding 扫描器侧对称条目本就 dormant（接口静态访问 `binding.Stat.ino` 在值位非法，永不命中），实测净增来自实例侧 66 条（`Stat`/`Stream`/`Watcher`/`ResultSet` 成员）。对称条目改要求 `repl.kit===dropin` 0 回归（11 条原抑制条目 repl.kit 全等于 dropin）。净增 auto +66（3272→3338），manual 1441→1375，chain-length-mismatch 573→507（cross-kit 31 不变）。剩余 manual 大头经深挖确认为调用形状真变化、不可安全自动替换：cross-kit 31 为 1-seg 歧义 `bluetoothManager.on/off`（10）+ 类成员随容器跨 kit 但 repl.kit 解析为非索引 kit 路径（如 `@ohos.application.Configuration.language`→`@ohos.app.ability.Configuration.language`，待 parser/抑制修正）；chain-length-mismatch 507 多为枚举↔枚举成员等语义变化（如 `Event`→`Event.USER_LOGIN`、`AbilityLifecycleState.UNINITIALIZED`→`AbilityLifecycleState`）；no_replacement 831 无 @useinstead。**round ⑩（2026-08-11）**：**①冗余 kit-namespace 前缀剥离（B1）+ ②verifyInstanceSafe 非对称扩展（B2）+ ③classifier instanceSafe 保真度修正**。真实 SDK 的 `@useinstead` 用 `Class#method` 记法（`#` 分隔 path 与 leaf，如 `ohos.pasteboard.pasteboard#createData`、`ohos.arkui.UIContext.Router#getStackSize`），round ⑥ 已滤 `#event:NAME` 含冒号碎片，但 `#member`（无冒号）保留→repl.members 多一段。其中两子形状：**(B1) namespace-function 改名**——repl.members=[kitLastSeg, leaf]（2 段，kitLastSeg===repl.kit 末段且===dep.exportName），如 `pasteboard.createHtmlData`→`pasteboard#createData`（repl=[pasteboard,createData]）。此 repl[0] 是冗余 kit 命名空间前缀（repl.kit 已编码 kit），剥后 [createData] 成 same-kit 1-seg leaf 改名→`auto_rename_member`。新增 `normalizeRedundantNamespacePrefix(dep, repl)`：repl.members.length===2 && repl.members[0]===repl.kit 末段 && repl.members[0]===dep.exportName && dep.members.length===1 → 剥为 [repl.members[1]]。18 条非对称 1→2 namespace-function 改名归 auto。**(B2) instance-method 改名**——repl.members=[Container, newLeaf]（2 段，Container===dep.exportName 是真实类/接口，如 `Router.getLength`→`Router#getStackSize`，dep.exportName=Router、dep.members=[getLength]、repl=[Router,getStackSize]）。`verifyInstanceSafe` 旧门 `dep.members.length < 2` 只验对称（dep.members>=2，typeHead=dep.members[0]）；非对称被拒→instanceSafe 未打→实例扫描器对 `router.getLength()` 报 manual。扩展：门改为 `dep.members.length === 0`（仅拒空），非对称时 typeHead=dep.exportName；repl.members[0]===typeHead 且新 leaf 是同封闭类/接口的 sibling 成员→打 instanceSafe。12/19 非对称 1→2 instance-method 条经验证归 auto（7 条新 leaf 非 sibling 留 manual，如 `Lone.oldMethod`→`Lone#newMethod` 但 Lone 无 newMethod 成员）。instance scanner 对 `router.getLength()`→`router.getStackSize()` 走 rename-member 自动拼。**③classifier 保真度修正**：round ④起 instanceSafe 标已存在（对称 1-seg-repl，~46 条），但 classifier 脚本缺 instanceSafe 分支→误归 chain-length-mismatch。三脚本（classify-doc/classify/classify-full）加 `if (c===null && e.instanceSafe) c="auto";` 分支后正确计入。**净增 auto +77（3338→3415）**：+30 真新增 scanner 覆盖（B1 18 + B2 12）、+47 classifier 保真度修正（既有 instanceSafe 条目由 chain-mismatch 重计入 auto，非新覆盖）。manual 1375→1298，chain-length-mismatch 507→431（cross-kit 31、no_replacement 831 不变）。**round ⑪（2026-08-11）**：**无 @useinstead 成员随 kit/容器迁移"保留验证"抑制（B3 陷阱守卫）**。深挖剩余 manual 后发现两个看似可批量转 auto 的大杠杆实为**陷阱**：①**kitmove-158**（迁移 kit 下无 @useinstead 成员）——新 kit 里这些成员**已被删除**（如 `@ohos.ability.wantConstant`→`@ohos.app.ability.wantConstant` 时 `Action` 枚举与 `ACTION_HOME` 在新 kit **不存在**），抑制会让 rewrite-import 把 binding 指向无此成员的 kit 致代码损坏；②**restate-prefix-170 / contact.[X,X]**——`@useinstead contact.addContact#addContact` 对应新签名 `addContact(context, contact, callback)` 加了参数，是**签名变更**，正确归 manual。安全杠杆是**逐成员 SDK 验证**：给 no-repl 成员（其 kit 经 `kitIndex[dep.kit].newKit` 整体迁移、或其容器经 `crossKitDropin` 同名跨 kit 迁移）新增 `verifyMembersPreservedByMove` 后置扫描——kitmove 时 1-seg 查成员是否新 kit 顶层导出、2-seg 查 `members[0]` 顶层且 `members[1]` 为该容器（enum/interface/class/namespace）的直接成员（`collectContainerMembers` 遍历新 kit 源 AST）；dropin 时容器=`dep.exportName`、leaf=`members[0]` 同法验证。**仅当成员确实保留**才打 `memberPreservedByMove`，scanner（binding 循环 `continue` + `instanceFinding` 返回 null）抑制（容器/kit 的 import 改写已覆盖）；**删除的成员留 manual**（重指 binding 会指向无此成员的 kit）。新增 `kitSourceFiles`（kit→SourceFile）与 `kitTopLevelExports` 同源收集供后置验证器下降容器。实测仅 4 条验证为真保留（`bundle.getAbilityInfo`/`rdb.ResultSet`/`storage.ValueType` 等——SDK 对保留成员通常另给 @useinstead 走既有路径，no-@useinstead 的多是删除的）。净增 auto +4（3415→3419，no_replacement 831→827），manual 1298→1294。增量虽小，但验证器把"kitmove-158 是陷阱"这一发现**编码为代码+回归测试**（`movedSdk` fixture：保留的 `Flags.F1/F3` 抑制、删除的 `Flags.F2` 与整枚举删除的 `Action.A/B` 留 manual；`dropinMovedSdk` fixture：`Stat.nlink` 保留抑制、`Stat.dev` 删除留 manual），防后续轮次误重试非安全抑制。**round ⑫（2026-08-11）**：**无 @useinstead 成员随同 kit 容器原地改名"保留验证"抑制（B3 陷阱守卫续）**。深挖剩余 manual 时发现另一批 no-`@useinstead` 2-seg 成员（`[C, M]`）的父容器 `[C]` 有**同 kit 1-seg 改名 repl `[C']`**（如 `huks.HuksResult`→`HuksReturnResult`、`huks.HuksErrorCode`→`HuksExceptionErrCode`、`media.AudioEncoder`→`CodecMimeType`）。初看 ~79 条候选可批量转 auto，但 SDK 验证后**绝大多数是陷阱**：①**命名约定整体变更**——67 条 `huks.HuksErrorCode.HUKS_*` 在新枚举 `HuksExceptionErrCode` 全改为 `HUKS_ERR_CODE_*` 命名，**无一条保留**；②**成员被删/改名**——`HuksHandle.errorCode`/`token`（新 `HuksSessionHandle` 无）、`HuksResult.errorCode`（新 `HuksReturnResult` 无）、`AudioEncoder.DEFAULT`（新 `CodecMimeType` 无 DEFAULT，改 `VIDEO_H263` 等）、`AudioOutputFormat.DEFAULT`（新 `ContainerFormatType` 改 `CFT_MPEG_4` 等）、`AuthenticationResult.CAMERA_FAIL`（新 `ResultCode` 无）、`UserAuth.getVersion`（新 `AuthInstance` 无）。扩展 `verifyMembersPreservedByMove` 增**第三形状**：2-seg 成员 `[C, M]` 父项有同 kit 1-seg 改名 `[C']` 时，用 `bySig` 查父项、验证 `M` 为 `C'`（`collectContainerMembers`）的直接成员——**仅保留才打 `memberPreservedByMove`** 抑制（容器改名的 1-seg repl 已改写 type/import binding，保留成员访问点无需拼改）；删除/改名的留 manual。实测仅 **4 条**验证为真保留（`huks.HuksResult.outData/properties/certChains` + `huks.HuksHandle.handle`，新接口确有这些字段）。净增 auto +4（3419→3423，no_replacement 827→823），manual 1294→1290。增量虽小但与 round ⑪ 同理：验证器把"same-kit 容器改名多伴成员删/改名"这一发现编码为 `sameKitRenameSdk` fixture 回归测试（保留的 `ErrorCode.SUCCESS` 抑制、改名的 `ErrorCode.FAILURE`→`FAILURE2` 留 manual），防后续误批量抑制。**round ⑬（2026-08-11）**：**跨 kit 冗余命名空间前缀剥离（leaf-preserved 门）**。深挖 chain-length-mismatch 431 桶发现 1→2 形状中一批 `@useinstead` 用 `ohos.<newKit>.<newNamespace>#<leaf>` 记法（如 `@system.router.Router.push`→`@ohos.router:router.push`、`@ohos.data.storage.storage.getStorageSync`→`@ohos.data.preferences:preferences.getPreferences`），repl.members=[<newNamespace>, <leaf>]，其中 `<newNamespace>` 是**新 kit 自身命名空间**（== repl.kit 末段）属冗余前缀。round ⑩ B1 的 `normalizeRedundantNamespacePrefix` 只剥 same-kit 形状（要求 `repl[0]===dep.exportName`，即冗余前缀==旧导出名），漏掉**跨 kit 形状**（冗余前缀==新 kit 命名空间、≠旧导出名，如 `router` vs `Router` 大小写都不同）。扩展：新增跨 kit 分支——`repl[0]===kitLastSeg && repl.kit!==dep.kit` 时剥为 `[repl[1]]`，交由既有 `crossKitMemberDropin` 验证 leaf 为目标 kit 顶层导出后注入新 import + 重绑 receiver（`Router.push`→`router.push`）。**关键安全门：仅 leaf 保留时剥离**（`repl[1]===dep.members[0]`）——跨 kit 且 leaf 改名常伴签名变更（如 `storage.getStorageSync`→`preferences.getPreferences` 加了 `Context` 参数且 sync→async，1-seg 重绑会错拼），故 leaf 改名留 2-seg chain-mismatch manual。leaf-保留跨 kit 重绑**严格安全于** round ③ 已发车的 leaf-改名跨 kit dropin（193 条）。实测净增 auto +7（`@system.router.Router.{push,replace,back,getParams,clear,getLength,getState}`→`@ohos.router` 同名 leaf）；3 条 `@system.storage.Storage.{get,clear,delete}`→`preferences` 虽 leaf 保留但 `get/clear/delete` 非 `@ohos.data.preferences` 顶层导出（是 Preferences 实例方法），验证器正确拒、留 manual（从 chain 桶改归 cross-kit 桶，覆盖与 manual 性不变）。`crossKitRenameSdk` fixture 把"leaf 保留→剥离→auto、leaf 改名→不剥→manual"编码为回归测试。**round ⑭（2026-08-12）**：**同 kit 1→2 嵌套容器 INSERT（静态方法门）**。深挖剩余 chain-length-mismatch 413 桶发现 1→2 形状中一批废弃的 kit 顶层函数 `@useinstead` 指向**同 kit 嵌套类的静态方法**（如 `@ohos.i18n.is24HourClock`→`i18n.System.is24HourClock`、`@ohos.UiTest.create`→`UiTest.Driver.create`），形状变化**仅是 leaf 前插入一段容器**（leaf 保留），scanner 可拼 `binding.<leaf>`→`binding.<container>.<leaf>` 并**复用原 kit binding**（无需注入 import，与 round ③/④ 跨 kit dropin 不同）。新增 `verifyNestedContainerInsert` 后置扫描 + `nestedContainerInsert` 标：候选须 same-kit 1→2 且 leaf 保留（`repl[1]===dep.members[0]`）、容器为干净标识符、**非歧义**（dep 符号→唯一容器）、容器为 dep.kit 顶层导出、容器是 CLASS 且 leaf 为其**静态方法**（新增 `memberIsStaticMethodOfClass`，用 ts-morph `ClassDeclaration.getMethods()` + `StaticKeyword`）。**五陷阱门**：①**实例方法**（`process.ProcessManager.isAppUid`——在类上调实例方法会坏）须 static；②**interface 容器**（`worker.WorkerEventTarget.*`——interface 类型无运行时值）须 class；③**restate-leaf/非容器**（`contact.addContact.addContact`——addContact 是 function 无成员）须容器有成员；④**stale @useinstead**（`i18n.set24HourClock`→`System.set24HourClock` 但 System 无此方法）须方法存在；⑤**歧义**（`UiTest.click`→Component.click 与 Driver.click 两条）须唯一。**short-form 处理**：SDK 的 5 条 i18n 用畸形短式 `ohos.System.getDisplayCountry`（`ohos.System` 非 kit→repl.kit 解析为 undefined），verifier 以"same-kit 意图"收纳（repl.kit 缺省视为 same-kit），由顶层导出门验证容器确在 dep.kit（真跨 kit 不解析的仍被拒）。**exportIndex 守卫**：若 `dep.exportName`（容器）已有同 kit 导出改名（如 `UiDriver`→`Driver`，`UiTest.create` 实为 `UiDriver.create`），rename-export 规则已重指 import binding 且 member finding 在 scanner `exportIndex` 守卫处抑制——此标会成死代码，故跳过（`UiTest.create` 经 export-rename 已 auto，不重复计入本轮）。`nestedInsertSdk` fixture 把"静态方法→auto、实例方法/interface/stale/畸形短式各→manual"编码为回归测试。净增 auto +8（8 条 i18n.System 静态方法：getDisplayCountry/getDisplayLanguage/getSystemLanguage/getSystemRegion/getSystemLocale/is24HourClock/getPreferredLanguageList/getFirstPreferredLanguage），manual 1283→1275，chain-length-mismatch 421→413。Tests 167→170。**round ⑮（2026-08-12）**：**跨 kit 容器导出假 leaf-改名修正（shape-change 守卫）+ 剩余 chain-mismatch/cross-kit 桶深挖定性为陷阱**。深挖剩余 manual 各桶后：(1)**2→1 flatten 桶**（skit 2→1 leafPres=1 的 41 + leafPres=0 的 53）——`resourceManager.ResourceManager.getStringValue` 的 `ResourceManager` 是 `interface`（实例方法），`binding.ResourceManager.getStringValue` 在值位非法、永不命中，且 26 条均为实例方法→trap；leaf-改名 2→1（`AudioManager.on→AudioRoutingManager`、`FeatureType.HCE→hasHceCapability`）repl 指向新容器**名**或枚举→函数的语义变化，非 drop-in→trap。(2)**1→2 leaf-改名 桶**（skit 97 + xkit 37）——`sensor.on→SensorId.ACCELEROMETER`（函数→枚举成员）、`bundle.isVisible→AbilityInfo.exported`（interface 实例属性）、`Event→Event.USER_LOGIN`（命名空间值→具体成员）、`contact.addContact→selectContact.selectContacts`（function-on-function）均为语义变化→trap；`@?shortcutInfo/GeolocationResponse/BackRouterOptions/CustomizeData` 等均 interface 实例属性（binding.Container.leaf 在值位非法）→trap。(3)**cross-kit 34 桶**——10 条 `bluetoothManager.on/off` 映射 connection/access/socket 三 kit 歧义（scanner 读不到事件名串选目标）；6 条 `fileio.Dir.{read,close}`→`listFile`（容器→leaf 语义重定向）；3 条 `storage.Storage.get` leaf 保留但 `get` 是 Preferences 实例方法非 `@ohos.data.preferences` 顶层导出（验证器正确拒）；9 条 `Want.*` interface 实例属性→trap。**结论：剩余 chain-length-mismatch 413 + cross-kit 34 桶经逐条定性几乎全为调用形状/语义/签名真变化或歧义，文本拼接式验证杠杆已挖尽**（auto 3433 即安全上限）。深挖中确证一处**既有 crossKitRenameExport 构建缺陷**：memberless 导出声明若同时是**容器**（interface/class/namespace，另有 member-level entry，如 `@ohos.fileio.Dir` 有成员 `Dir.read`），其 `@useinstead` 指向 leaf（`listFile`）时，旧代码内联构建 `crossKitRenameExport[Dir]=@ohos.file.fs\0listFile`，令 scanner 产 `import {Dir}`→`import {listFile as Dir}` 别名——把类型名绑到函数值，破坏 `let d: Dir` 类型用法与 `Dir.read()` 成员访问。真实 map 实测 35 条此类假映射（`fileio.Dir/Dirent`、`cipher.CipherResponse`、`geolocation.GeolocationResponse`、`router.BackRouterOptions`、`sensor.*`、`featureAbility.CustomizeData/ModuleInfo` 等）。**修正**：将 `crossKitRenameExport` 构建由内联（descendant 循环内，此时 member entry 尚未全产出、容器集不可得）改为**后置扫描**——先收集候选 `{key, replKit, newName}`，循环结束后用全量 `entries` 算 `containerExports`（有 member entry 的 `kit\0export`），**跳过容器**（容器→异名 leaf 是 shape change 留 manual），仅 leaf 导出（无 member entry，如 `fileio.fstat→file.fs.stat`）入映射。**仅 crossKitRenameExport 受限**：crossKitDropin（同名，容器整体迁移保形）与 exportIndex（同 kit 容器改名 `UiDriver→Driver`，类→类保形）经核实保形、不受影响。净 **auto −5（3438→3433）**：5 条无 kitmove 的容器导出（`fileio.Dir/Dirent`、`featureAbility.CustomizeData/ModuleInfo` 等）由假 auto 归 manual（export-other 5→10）；另 30 条 `@system.*` 容器虽因 kitmove 分支仍计 auto，但**不再产 `import {leaf as Container}` 破坏性别名**（既有正确性缺陷消除，metric 降但保真）。`fileio.d.ts` fixture 把"容器 Dir 不入映射、leaf fstat 入映射"编码为回归测试。Tests 170→171。**round ⑯（2026-08-12）**：**aligned kit-move 实例属性改名"新 kit 容器验证"（补 verifyInstanceSafe 的 kit-move 盲区）**。round ⑮ 结论称"剩余 chain-length-mismatch 413 几乎全为调用形状/语义真变化、auto 3433 即安全上限"，但深挖 1→2 桶发现一批**被该结论误盖**的 sound 子集：aligned kit-move（`kitIndex[dep.kit].newKit===repl.kit`）下、`@useinstead` **重述未变容器类型**的实例属性改名（如 `@ohos.bundle`→`@ohos.bundle.bundleManager` 整 kit 迁移时，`AbilityInfo.isVisible`→`[AbilityInfo,exported]`、`AbilityInfo.launchMode`→`[AbilityInfo,launchType]`、`AbilityInfo.metaData`→`[AbilityInfo,metadata]`、`AbilityInfo.bundleName`→`[AbilityInfo,bundleName]` leaf 保留）。这些**非调用形状变化**——receiver 类型不变（rewrite-import 重指 binding 后实例访问 `ai.isVisible` 仍在重指后的 binding 上，仅 leaf 名变或不变），与 round ⑮ 归为 trap 的 `sensor.on→SensorId.ACCELEROMETER`（函数→枚举成员）等语义变化本质不同。**盲区根因**：内联 `verifyInstanceSafe`（index 循环内，line ~657）验新 leaf 是否为**废弃符号自身封闭类/接口**的成员——kit-move 时新 leaf 活在**新 kit 的容器**里（常在另一嵌套文件，如 `bundleManager/AbilityInfo.d.ts` 的 `interface AbilityInfo`），旧 kit 文件查不到→返回 false→不打 `instanceSafe`→实例扫描器对 `ai.isVisible` 报 manual。**修正**：新增后置扫描 `verifyInstanceSafeOnKitMove`——仅 aligned kit-move（newKit===repl.kit）且 repl 形状保类型（1-seg `[newLeaf]` 或 2-seg `[Type,newLeaf]` 且 `Type===container`，跨容器重定向如 `BundleInfo.type→[ApplicationInfo,bundleType]` 不保类型、留 manual）时，用新 `containerHasMemberInKit(newKit, container, newLeaf)` 验新 leaf 为新 kit 容器成员（**删除的成员验失败→留 manual**，与 round ⑪/⑫ B3 陷阱守卫同纪律），通过则打 `instanceSafe`，复用既有实例扫描器分支（line ~785 splice `var.<oldLeaf>→var.<newLeaf>`；line ~780 leaf 等值 no-op 抑制）。**基础设施**：原 `kitSourceFiles`（kit→单 SourceFile）只含 kit 顶层文件，找不到经 re-export 聚合的嵌套容器 body（如 `@ohos.bundle.bundleManager` 的 `export type AbilityInfo=_AbilityInfo.AbilityInfo` 是类型别名，真 `interface AbilityInfo {...}` body 在嵌套文件 `bundleManager/AbilityInfo.d.ts`）→新增 `kitToSourceFiles`（kit→SourceFile[]，主循环内从**所有**经 re-export 追溯归属的文件累积，跳过 `@?` 孤儿）；`memberResolvesInKit`/新增 `containerHasMemberInKit` 遍历该数组下降容器，亦**单调提升 `memberPreservedByMove` 覆盖**（原只查顶层文件）。**实测**：aligned kit-move 实例条目共 1260，其中 914 打 `instanceSafe`——但**真实新增覆盖仅 +46**（全在 `@ohos.bundle`→`bundleManager` 集群：AbilityInfo/UsedScene 等接口的实例属性），余 868 为**冗余 inert 标**（1-seg leaf-保留的 kit-move 命名空间函数，如 `dataUriUtils.getId`→同 leaf，已由 binding 扫描器 no-op 抑制归 auto，实例扫描器永不命中——命名空间函数非实例访问——故打 `instanceSafe` 无副作用亦无覆盖增量）。`memberPreservedByMove` 由 4→8（多文件下降多验出 4 条 no-`@useinstead` 保留成员）。**净增 auto +46（3433→3479，72.8%→73.9%）**，manual 1280→1234（chain-length-mismatch 413→367，cross-kit 34/export-other 10/no_replacement 823 不变）。`kitmoveInstanceSdk` fixture 把"renamed leaf→instanceSafe splice、preserved leaf→suppress、removed leaf→manual"三态编码为回归测试（indexer 断言 + scan+rewrite E2E）。Tests 171→173。**round ⑰（2026-08-12）**：**type-alias re-export 的容器 body 别名跟随**（补 round ⑯ `verifyInstanceSafeOnKitMove` 的 kit-move 实例盲区之盲区）。round ⑯ 的 `containerHasMemberInKit(newKit, container, leaf)` 遍历 `kitToSourceFiles[newKit]` 下降容器 body 验新 leaf，但该 Map 由主循环 `resolveOwnKit`（first-writer-wins）归属：真实 SDK 中 `bundleManager/ApplicationInfo.d.ts`（`interface ApplicationInfo { name, description, ... }` body）被 `@ohos.bundle.launcherBundleManager` 先经 namespace import 归属，故 `kitToSourceFiles[@ohos.bundle.bundleManager]` 只有顶层文件——其 `export type ApplicationInfo = _ApplicationInfo` 是**类型别名**，`collectContainerMembers` 不收 TypeAlias→查不到成员；`AbilityInfo` 等 body 直接归属同 kit 的能验过，`ApplicationInfo` body 跨 kit 归属的验失败→留 manual（round ⑯ +46 漏的 ApplicationInfo 实例属性）。**修正**：新增后置扫描 `addTypeAliasReExportedFiles`（在 `verify*` 前跑）——对每 kit 归属文件，找 EXPORTED `export type C = <ref>` 别名，解析 `<ref>` 首标识符为本地导入名（namespace import `import * as _X` 或 named import `import { C as _C }`，**关键：取 `getAliasNode()` 本地名而非 `getName()` 源名**——后者对 `import { ApplicationInfo as _ApplicationInfo }` 返回源名 `ApplicationInfo` 而非别名 `_ApplicationInfo`），由导入 module specifier `resolveRelative` 到 body 文件加入该 kit 的 `kitToSourceFiles`。**仅 EXPORTED 别名、仅引用本地导入名**（不跟内部 type alias / 非再导出 import）→无私有面膨胀；`memberPreservedByMove`/`verifyInstanceSafeOnKitMove` 均单调受益（只加文件、只增标）。实测净增 auto +16（全 `@ohos.bundle`→`bundleManager` 的 ApplicationInfo 实例属性：15 leaf-保留抑制 + 1 leaf-改名 `metaData→metadataArray` splice；5 条无 repl 或跨容器重定向 `moduleInfos→BundleInfo.hapModulesInfo` 留 manual）。`kitmoveAliasReExportSdk` fixture 把"body 经 named-import type-alias 再导出、归属他 kit→别名跟随后才验过"编码为回归测试（indexer + scan+rewrite；关闭 post-pass 即两测试 fail 证其为真守卫）。Tests 173→175。**round ⑱（2026-08-12）**：**跨 kit 2→1 命名空间 flatten**（补 round ⑥ 把所有 2→1 当 call-shape-change 拒掉过保守的部分）。真实 SDK 的 `bluetoothManager.BLE.startBLEScan`→`bluetooth.ble.startBLEScan`：`BLE` 是 **namespace**（运行时值，`bm.BLE.startBLEScan()` 是合法值位访问），其成员整批迁到 `@ohos.bluetooth.ble` 顶层函数，repl 1-seg `[startBLEScan]`（leaf 保留且为目标 kit 顶层导出）——scanner 的 `crossKitMemberDropin` 分支本就 length-agnostic（`oldSymbol=bm.BLE.startBLEScan`→`replacement=ble.startBLEScan`，namespace 段自然丢），无需改 scanner。**修正**：`verifyCrossKitMemberDropin` 的等长门放宽——等长（N→N）**或** 2→1 flatten（dep 2-seg `[C,leaf]`、repl 1-seg `[leaf]`）皆放行；其余不等长（1→2 extend、2→3 等）仍拒。**关键安全门：2→1 时被丢的容器须为 ownKit 的 NAMESPACE**（`containerIsNamespaceInKit`，查 `kitSourceFiles[ownKit]` 的 ModuleDeclaration）——interface/class 容器（如 `GattServer`、`PhotoAsset`、`BaseProfile`）的 `binding.Container.leaf` 是 inert（实例成员经类型名不可达，永不命中真代码），打标会**虚增 auto 无真覆盖**（round ⑮ 已定性 2→1 interface-instance 为 trap）；enum 容器（`PhotoViewMIMETypes`/`PowerModel`）的 repl 是 member→enum 重定向（非 leaf-保留 flatten）亦拒。repl leaf 仍须为目标 kit 顶层导出（既有门）。实测净增 auto +7（全 `bluetoothManager.BLE.*`→`bluetooth.ble.*`：createGattServer/createGattClientDevice/getConnectedBLEDevices/startBLEScan/stopBLEScan/on/off，全 leaf-保留）；18 条非 namespace（GattServer/BaseProfile interface、PhotoAsset/PhotoSelectOptions/PhotoSelectResult interface、PhotoViewMIMETypes/PowerModel enum 重定向）正确拒、留 manual。`flatten21Sdk` fixture 把"namespace 容器→flagged、interface 容器→rejected"编码为回归测试（indexer + scan+rewrite；关 namespace 门即 interface-trap 测试 fail 证其为真守卫）；round ⑥ 原"BLE.on 2→1 stays manual"测试更新为"2→1 namespace flatten flagged"（toReplSymbol 滤冒号碎片后 repl=[on]→flatten→flagged，冒号滤除仍是必要前提否则 IDENT_RE 拒）。Tests 175→177。**round ⑲（2026-08-12）**：**同 kit 2→1 命名空间 flatten 候选深挖=空集（陷阱定性）**。round ⑱ 把跨 kit 2→1 namespace flatten 发车后，检查同 kit 2→1（`binding.C.leaf`→`binding.leaf`，repl.kit 空或=dep.kit，leaf 保留）是否有对称 sound 子集。dump 出 41 条未标 d2→r1 skit leafPres=1，分布：ResourceManager 26、Character 9、InputMethodEngine 4、I18NUtil 1、UserAuth 1。逐容器查 SDK：`ResourceManager` 是 `interface`（@ohos.resourceManager.d.ts:843）、`I18NUtil`/`Character` 是 `class`、`InputMethodEngine` 是 `interface`、`UserAuth` 是 `class`——**无一为 namespace**，全是实例方法持有者；且 `getStringValue` 等 leaf 在本 kit 无顶层导出（@useinstead `ohos.resourceManager.getStringValue` 指向的顶层函数本 SDK 不存在，是 instance→static 的语义重定向）。故同 kit 2→1 flatten 全为 instance→static 调用形状变化（receiver 丢、参数变），非文本拼接；round ⑱ 的 `containerIsNamespaceInKit` 门若推广到同 kit 亦正确拒全部 41 条。**结论：同 kit 2→1 flatten 杠杆为空，剩余 ~1211 manual（no_replacement 823、chain-length-mismatch 344、cross-kit 34、export-other 10）经 ⑮-⑲ 逐条深挖确认为调用形状/语义/签名真变化或歧义或无目标，文本拼接式增量杠杆已挖尽；进一步覆盖需代码合成式大特性（UIContext receiver 配方合成、contact 签名变换、huks 枚举值映射等），非本轮文本拼接范式。**round ⑳（2026-08-13）**：**忠实分类器重建 + UIContext 配方簇定性为陷阱**。原 classify-*.mjs scratch 脚本已删，doc 桶数（1211）不可复现，故重建 `scripts/classify-faithful.mjs`——镜像 scanner+indexer 完整决策树（indexer flags、exportIndex 抑制 line 144、`memberCoveredByContainerDropin` sym+asym 含 `repl.kit===dropin` 守卫、kit module-move 覆盖 export-level、`describeMemberReplacement` no-op/aligned 抑制+rename-member+cross-kit/unresolved manual），产出可复现的 manual 形态拆解。重建后得 auto 3441 / manual 1272（与 doc 1211 差 61，全在 export-other 桶：doc 桶数本就已知不准——删除分类器的产物；faithful 版为 lever 搜寻的 source of truth）。用 faithful 列表逐最大 leaf-preserved 形态 dump 验证：**74 条 cross_kit d1→r2 xkit leafPres=1 全是 `UIContext.<SubController>.<method>`**（componentSnapshot.get→UIContext.ComponentSnapshot.get、dragController.executeDrag→UIContext.DragController.executeDrag、font.registerFont→UIContext.Font.registerFont、router.pushUrl→Router.pushUrl 等，共 57 条 repl.kit=@ohos.arkui.UIContext + 同构跨 kit），目标是 UIContext 子控制器的**实例方法**，须合成 receiver（`this.getUIContext()` 或 `getContext().getUiContext()`），作用域 context-dependent、不可由 SDK 结构验证；**56 条 skit d1→r2 leafPres=1 全是 `contact.addContact`→`addContact.addContact` restate**（function-on-function 签名变更，round ⑪ 已定性）。**结论：剩余 ~1272 manual 经 faithful 分类器逐形态确认为 no_replacement 752（删除 API 无目标）、UIContext receiver-合成 74（context-dependent）、contact 签名变更 56、cross-kit 歧义/实例方法/枚举重定向 196、chain-length 形态变化 243、export-other 81（删除导出/容器 shape-change），无文本拼接 sound 子集。UIContext 簇（57 条）是唯一有规模的方向，但其 receiver 合成本质 manual（stageless 架构强制开发者显式获取 UIContext），自动合成产不可验证代码。faithful 分类器已 commit 为永久工具，后续轮次不再遇"数字不可复现"问题。**

**嵌套 namespace module-move bug（已修）**：indexer 原以 `isNamespaceLevel`（node 为 ModuleDeclaration）判 kit 级 move，但嵌套 namespace（如 bluetoothManager 内的 `namespace BLE`）也是 ModuleDeclaration，其成员的 cross-kit @useinstead 被错记为整 kit 的 newKit。修正：要求该 namespace 无外层 ModuleDeclaration 祖先（即文件最外层 kit 命名空间）才记 kitIndex module-move/manual。

**@useinstead 后缀解析**：解析器原只认 `ohos.<完整点分 kit>` 前缀。SDK 偶有简写/缺中段形式（`ohos.distributedDataObject.create` 实指 `@ohos.data.distributedDataObject`；裸 `distributedDataObject`）。新增 `resolveBySuffix`：当 head 是唯一已知 kit 点分名的末段（大小写不敏感）时解析为该 kit。`ohos.` 分支在 longestKitPrefix 失败时回退到后缀解析；裸标识符分支同样回退。unresolved 48→7。保守：多义不解析。

测试：`npm test`（node --test，177 个，全部通过）。SDK 路径见 [[harmonyos-sdk-location]]。

**实例方法检测（type-aware）**：两层。①regex 增量 1：`scanProjectInstanceMembers` 用正则从 import 解析变量类型（`ns.Type` 限定式 / `T` 命名导入，同时支持 .ts/.ets 无 tsc），匹配带类型注解的本地变量/参数作 receiver 的实例方法。indexer 用 **sibling check**（在 SDK AST 里查 repl leaf 是否为同接口/类成员）打 `instanceSafe`：支持两种 repl 形状——单段 `[newLeaf]`（如 getString→getStringValue）与**类型保留两段** `[Type,newLeaf]`（repl[0]===dep 类型，如 Window.show→Window.showWindow，仅 leaf 变）。已验证→rename-member 自动拼 `var.leaf→var.replLeaf`（真实 map 256 条，较增量1 的 46 条 +210，覆盖 window/rpc/account.appAccount/account.osAccount/audio 等）；未验证/跨 kit→manual finding 报 @useinstead 目标（不再静默漏报）。**容器 crossKitDropin 抑制**：实例成员若其容器（exportName）经 crossKitDropin 跨 kit 同名迁移、且 repl.kit===dropin 目标、链全等，则 `instanceFinding` 经 `ContainerDropinResolver` 返回 null 抑制（容器 import 已被 rewrite-import 改写覆盖，实例访问在重指后的 binding 上自然解析，不再冗余报 manual）。②tsc 增量 2：`scanProjectInstanceMembersTsc` 用 ts-morph 对项目 **.ts** 文件（.ets 排除，struct/build() tsc 解析不了）做类型检查，解析无类型注解的运行时 receiver（`const ctx = featureAbility.getContext()` 同步、`const win = await window.getLastWindow()` 经 await 解开 Promise）。kit 归属用 indexer 新持久化的 **fileKit**（文件→kit 映射，API 24 共 695 条，与 dep 条目同源归属保证 key 一致），辅以 `declare module` 回退（合成 fixture）。复用 instance index + instanceSafe 分类。SDK 缺失或无 .ts 文件→静默降级（regex scanner 仍为基准）。真实 SDK 实测：ctx.setShowOnLockScreen/win.show→manual 检测、rm.getString→rm.getStringValue 自动（皆由静默漏报转为检出）。仍漏：.ets 中的无注解 receiver、多段 repl 路径在实例上、跨 kit FA→stageless 自动改写（receiver 合成）。

**fileKit 映射**：`DeprecationMap.fileKit: Record<string,string>`，key 为 SDK api 树内相对路径（正斜杠），value 为 indexer re-export 追溯出的归属 kit（孤儿文件为 `@?`）。tsc scanner 据已解析类型的声明文件回查 kit。另需 `paths` 映射（`@ohos.*`/`@system.*`→`./@ohos.*.d.ts`）+ baseUrl= sdkApiDir，使默认导入解析真实 SDK 的 module 式声明文件（`declare namespace X` + `export default X`，非 `declare module`）。

测试：`npm test`（node --test，177 个，全部通过）。SDK 路径见 [[harmonyos-sdk-location]]。
