---
name: deprecated-extraction-script
description: ohos-migrator 项目结构与废弃函数迁移规则集（ts-morph 索引 + 扫描 + 重写管线）
metadata:
  node_type: memory
  type: project
  originSessionId: 34643758-d30e-4893-9711-e6f7576f09ce
  modified: 2026-08-05T16:52:09.129Z
---

ohos-migrator 项目位于 `/Users/aha/code/ohos-migrator`，是鸿蒙废弃函数迁移工具（非早期的独立 `extract_deprecated_ts.js`，已废弃）。3 阶段管线：index(ts-morph) → scan(正则/offset tolerant) → rewrite(自底向上 offset 拼接)。

**索引**：`src/indexer/sdk-indexer.ts` 递归收集 `.d.ts/.d.ets`，跳过 `@internal/`；嵌套文件经 re-export 追溯归属顶层 kit；输出 `DeprecationMap`（entries + kitIndex + exportIndex），缓存于 `.harmony-deprecate/deprecation-map.<apiVersion>.json`（API 24 = 4713 条，其中带 @useinstead 3891 条）。

**规则种类（RuleKind）与自动可替换性**：
- `rewrite-import` —— kit 整体迁移（kitIndex[oldKit].newKit）改 import specifier；或**跨 kit 命名导入 drop-in**：named-import 子句中所有 binding 都同名迁移到同一目标 kit 时改 specifier 保名（per-clause 安全，混合目标/含无替代项的子句不动）。由 `crossKitDropin` 索引驱动。**默认导出迁移**（`export default class X` 跨 kit，indexer 用 `hasDefaultModifier` 检测，键为 `${kit}\0default`）也走 drop-in：`import X from '@old'`→`import X from '@new'`（仅改 specifier，本地 binding 是 default 导出，保名，调用点不动）。scanner 的 `scanProjectCrossKitDropin` 统一处理 default+named binding（per-clause：所有 binding 含 default 须同目标 kit，否则整子句不动；default binding 不可别名，只改 specifier）。
- `rename-member` —— 同 kit（或 kit-move 对齐）且**替换链与废弃链等长**的改名：把 `binding.<old chain>` 整条拼成 `binding.<new chain>`。覆盖 leaf-only、container/mid-segment、container+leaf 三类（如 `rpc.MessageParcel.create`→`rpc.MessageSequence.create`、`media.MediaErrorCode.MSERR_OK`→`media.AVErrorCode.AVERR_OK`）。链长不等→manual（调用形状变了）。亦用于**跨 kit 等长成员迁移**（kit 未整体迁移、repl.kit 是经 indexer 验证的真实 kit 且 repl 链首段为其顶层导出，entry 标 `crossKitMemberDropin`）：scanner 注入 `import * as <新binding> from '<repl.kit>'` 并把调用点 receiver 重绑到新 binding、整条 repl 链拼出。覆盖 1-seg 单 leaf（如 `particleAbility.startBackgroundRunning`→`backgroundTaskManager.startBackgroundRunning`）与**多段路径保留迁移**（2-seg，链全等、仅 kit 变，如 `bluetoothManager.A2dpSourceProfile.connect`→`bluetooth.a2dp.A2dpSourceProfile.connect`、`bluetoothManager.HandsFreeAudioGatewayProfile.disconnect`→`bluetooth.hfp.HandsFreeAudioGatewayProfile.disconnect`），以及 container/leaf 单段改名的跨 kit 等长迁移（如 `SppOption.uuid`→`SppOptions.uuid`）。repl 各段须为干净标识符；@useinstead 的 `name:value` 事件提示碎片（如 `connection.on#event:bluetoothDeviceFind` 解析出 `["on","event:bluetoothDeviceFind"]`）由 `toReplSymbol` 滤除冒片段后还原为真链 `["on"]`（见下文 round ⑥）。**歧义门**：同一废弃符号 `(dep.kit, dep.members)` 映射到 >1 个 repl.kit 时（如 `bluetoothManager.on` 按事件分到 connection/access/socket 三 kit），scanner 只匹配符号无法读事件名串选目标，自动重绑会乱选→indexer `resolveCrossKitMemberAmbiguity` 把这类 entry 的 `crossKitMemberDropin` 撤标留 manual；唯一目标的符号保持 drop-in。
- `rename-export` —— 同 kit 导出/类改名，用安全别名保本地 binding：`{By}`→`{On as By}`，body 不改。exportIndex 由 indexer 构建。**跨 kit 不同名导出**（`crossKitRenameExport: ${oldKit}\0${old} -> ${newKit}\0${new}`）也走别名：specifier 改到新 kit 且 binding 别名为 `{new as old}`（如 `@ohos.fileio.fstat`→`@ohos.file.fs/stat`，`import { fstat }`→`import { stat as fstat } from '@ohos.file.fs'`，调用点不变）。per-clause：子句所有 binding 须迁到同一目标 kit（同名 dropin 或异名 rename 混合均可），否则整子句不动。由 `scanProjectCrossKitDropin` 统一处理（同名只改 specifier、异名额外发 rename-export finding 别名 binding）。
- `override` —— UIContext `get<Head>()` recipe 与 `@ohos.window` WindowStage/Window recipe（recipe 表见 `src/rewriter/overrides.ts`，跨 kit 才触发）。
- `inject-import` —— 跨 kit 等长成员迁移的"注入新 import"部分（与上面 rename-member 的 rebind finding 配对）：scanner 每 file 合并一次，在 import 块末尾零长插入新 import 行；rewriter 底向上 offset 拼接处理纯插入。命名冲突时自动加数字后缀（`a2dp`→`a2dp2`）；目标 kit 已被该 file 导入则复用既有 binding 不注入。一 file 内多个不同目标 kit（如 bluetooth.a2dp + bluetooth.hfp）分别注入各自 import。**零长 inject 与 rebind 同 offset 修正**：当废弃调用点紧跟 import 块下一行首时，inject 锚点（最后 import 行后换行处）与 rebind 匹配 offset 重合，rewriter 的 `dedupeOverlapping` 原把零长 inject 误判为"与 rebind 重叠"而丢弃（漏注入、`a2dp` 变未定义）。修正：零长插入（start===end）不占字符，永不因重叠丢弃；且底向上排序 tie-break `b.end-a.end`，使同 offset 处先应用 replacement（消费 [X,X+len]）再应用 insertion，避免顺序倒置错切文本。
- `manual` —— 其余。

**索引 kit 范围**：顶层 `@ohos.*` 与 `@system.*`（API 9 前遗留系统 kit，同为可导入顶层模块）。`@?` 合成 kit 仅用于无 importer/re-exporter 的真孤儿嵌套文件（已从 495 降到 23）。`@useinstead` kit 前缀大小写不敏感解析（kitLookup）。re-export 追溯为 fixpoint worklist：处理顶层 kit 的 `import` 与 `export {X} from`/`export *`，并递归处理被归属的嵌套文件的再导出（解析 `@ohos.bundle → bundle/bundleInfo → bundle/hapModuleInfo` 这类二级嵌套）。

**嵌套匹配去重**：SDK 常同时废弃类与成员（`rpc.MessageParcel` 与 `rpc.MessageParcel.create`），二者正则命中同一调用点。scanner 每 span 只保留最长 finding，rewriter 丢弃重叠 edit，避免拼接两次导致文本损坏（曾产出 `createte`）。

**kit-move 对齐（重要）**：跨 kit 成员若 `kitMove(dep.kit)===repl.kit`，则 rewrite-import 已重指 binding，视为"等效同 kit"：链不变→抑制（去除 ~1225 误报 manual）；等长链变→rename-member（如 bluetooth.getProfileConnState→getProfileConnectionState）。

**大小写不敏感 kit 解析**：useinstead-parser 用 lowercased→real kitLookup，修正 SDK 中 `ohos.uitest.Component` 这类错大小写限定符，使 UiComponent→Component / UiDriver→Driver 进入 exportIndex。

**分类现状**（API 24 真实 map 4713 条，带 @useinstead 3891 条；2026-08-11 精算，口径=镜像 scanner/rewriter 实际分类）：auto/covered 3415 条（72.5%）；manual 1298 条（27.5%：no_replacement 831、chain-length-mismatch 431、cross-kit 31、export-other 5）。六轮冗余抑制/扩展已落地：①自指 no-op（sameKit 且链全等，原 90 条）归 rewrite-import suppressed 不报；②已被 crossKitDropin specifier 改写覆盖、链相同的成员（原 11 条：Configuration.language/.colorMode + 9 个 Want 成员）在 scanProjectMembers 抑制；**round ⑤（2026-08-10）**：将此抑制推广到**实例扫描器**（scanProjectInstanceMembers + tsc 变体）——给 `instanceFinding` 加 `ContainerDropinResolver`，当成员的容器经 crossKitDropin 跨 kit 同名迁移、且成员 repl.kit===dropin 目标、链全等时返回 null 抑制。消除 `cfg.language`/`w.deviceId` 之类"实例访问被报 manual、而容器 import 已被 rewrite-import 改写覆盖"的冗余误报（真实 SDK 实测：单文件 8 finding→2，6 条冗余 manual 消失，0 manual 遗留）。per-entry 计数不变（11 条 round ② 已归 auto），是 scanner 输出保真度修正。③**跨 kit 单 leaf 成员迁移**（indexer 用 `kitTopLevelExports` 验证 repl leaf 为目标 kit 顶层导出后打 `crossKitMemberDropin`，193 条，如 particleAbility→backgroundTaskManager）：scanner 注入新 import + 重绑 receiver，由 manual cross-kit 转入 auto。④**跨 kit 等长多段成员迁移**（`crossKitMemberDropin` 由 1-seg 推广到 N→N 等长；验证 repl 链首段为目标 kit 顶层导出、各段须为干净标识符拒 `event:BLEDeviceFind` 解析碎片，329 条，如 bluetoothManager.A2dpSourceProfile.connect→bluetooth.a2dp.A2dpSourceProfile.connect、bluetoothManager.HandsFreeAudioGatewayProfile.disconnect→bluetooth.hfp...；其中 317 条链全等零改名、12 条 container/leaf 单段改名、2 条解析碎片留 manual）：同样注入新 import + 整条 repl 链重绑 receiver。**round ⑥（2026-08-10）**：**@useinstead 事件提示碎片滤除 + 歧义门 + rewriter 同 offset 注入修正**。`toReplSymbol` 滤除 repl.members 中含冒号的段（JS 标识符不含冒号，`:` 段必是 `#event:NAME` 解析碎片，如 `connection.on#event:bluetoothDeviceFind`→`["on","event:bluetoothDeviceFind"]`→还原 `["on"]`），使 56 条原链长不等（碎片多一段）的 cross-kit 条目还原为等长路径保留迁移、经验证后打 `crossKitMemberDropin` 转入 auto；其中 10 条 1-seg `bluetoothManager.on/off` 映射到 connection/access/socket 三 kit（歧义），`resolveCrossKitMemberAmbiguity` 后置扫描按 `(dep.kit, dep.members)` 聚合 repl.kit，>1 目标则撤标留 manual（scanner 只匹配符号无法读事件名串选目标，自动重绑会乱选）。净增 auto +46（522→568 flagged，56 滤除−10 歧义）、manual cross-kit bucket +10（碎片→等长但歧义）、chain-length-mismatch −56（碎片多一段→还原等长）。另修正 rewriter `dedupeOverlapping` 在 inject 锚点与 rebind 同 offset（调用点紧贴 import 块下一行首）时丢弃零长 inject 的 bug（零长插入不占字符不因重叠丢弃 + 底向上 tie-break 保 replacement 先于 insertion）。**round ⑦（2026-08-10）**：**模块级声明收集修正 + 跨 kit 成员 drop-in 与同名导出 drop-in 冲突门**。indexer 原 `collectKitTopLevelExports` 只进入首个 `declare namespace` body 收集顶层导出名，漏掉与 namespace 平级的模块级 `declare function`/`const`/`class`/`enum`（如 `@ohos.file.fs.d.ts` 同时有 `declare namespace fileIo` 与模块级 `declare function moveFile`/`listFile`/`access`，旧代码只收 `OpenMode` 漏掉这三个→无法验证 `system.file.move→file.fs.moveFile`）。修正：遍历 `sourceFile.getStatements()`，namespace 声明进其 `ModuleBlock` 收集、非 namespace 顶层语句直接收名（含 `declare function`、`VariableStatement` 取首声明名）；使 12 条系统 file.* 同名模块级 drop-in 正确验证、打 `crossKitMemberDropin` 转入 auto。**冲突门**：`@ohos.fileio` 的模块级 `read` 是同名导出 drop-in（`fileio.read→file.fs.read`，exportIndex/crossKitDropin 改 specifier 保名，是符号 `fileio.read` 的权威替代），但同名 1-seg 成员 `fileio.read→file.fs.listFile`（语义重定向、leaf 变）若也打 `crossKitMemberDropin` 标会令 namespace 用法 `fileio.read(...)` 错重绑到 `listFile`。新增 `resolveCrossKitMemberExportConflict`：1-seg 成员 entry 的 leaf 若存在同名 crossKitDropin 且 repl.kit===dropin 目标、repl leaf 与 leaf 不同→撤标留 manual（语义重定向非 drop-in）；2-seg 路径保留迁移不受影响。净增 auto +19（`crossKitMemberDropin` 568→588，+20 新标其中 1 条原已属 auto 故净 +19），manual 1469→1450。**round ⑧（2026-08-10）**：**嵌套文件导出索引门放宽**。indexer 原 export-rename / crossKitDropin / crossKitRenameExport 构建块以 `isTopLevel(filePath)` 守卫，只处理 kit 顶层文件（`@ohos.*.d.ts`/`@system.*.d.ts`）内的 memberless 导出声明，漏掉 kit 通过 re-export 聚合的嵌套文件导出（如 `@ohos.arkui.modifier` 经 `export { NavigatorModifier } from './arkui/NavigatorModifier'` 聚合的 `NavigatorModifier`、`@ohos.ability.featureAbility` 经 `export { ElementName } from './bundle/elementName'` 聚合的 `ElementName`/`CustomizeData`/`ModuleInfo`、`@ohos.settings` 聚合的 `ResultSet`）。这些嵌套文件的 memberless 导出声明经 `resolveOwnKit` re-export 追溯已归属到真实可导入 kit（非 `@?` 孤儿），是 kit 顶层导出，应享受与顶层文件声明相同的 exportIndex/crossKitDropin/crossKitRenameExport 处理。修正：守卫由 `isTopLevel(filePath)` 改为 `!ownKit.startsWith("@?")`（真实 kit，含顶层文件 + re-export 归属的嵌套文件），`!isNamespaceLevel` 仍排除 kit namespace 内的子命名空间（如 bluetoothManager 内的 `BLE`）。净增 auto +9（export-level manual 14→5：4 个 arkui.modifier 同名导出改名→exportIndex、1 个 arkui.node 改名→exportIndex、settings/ResultSet + featureAbility/ElementName 跨 kit 同名→crossKitDropin、featureAbility/CustomizeData + ModuleInfo 跨 kit 异名→crossKitRenameExport），manual 1450→1441。**round ⑨（2026-08-11）**：**非对称 1→2 容器 drop-in 抑制**。round ②/⑤ 的容器 drop-in 抑制只认**对称**形状（容器在 `dep.exportName`、repl 链==dep 链、容器随 crossKitDropin 同名迁移、repl.kit===dropin）。但真实 SDK 另有一批**非对称**形状：容器（类/接口，如 `fileio.Stat`/`ResultSet`/`Stream`/`Watcher`）在 `dep.exportName`、leaf 在 `dep.members`（1 段），repl **重述容器**为 `repl.members=[容器, leaf]`（如 `fileio.Stat.ino`→`file.fs.Stat.ino`，dep.exportName=Stat、dep.members=[ino]、repl.members=[Stat,ino]）。容器经同名 crossKitDropin 整体迁移后，实例访问 `s.ino` 在重指后的 binding 上自然解析，无需成员级拼改；但旧抑制的等长门（`rm.length===dm.length`）拒非对称→实例扫描器对 `s.ino` 仍报冗余 "wiring changes" manual。新增共享 `memberCoveredByContainerDropin`：要求 `repl.kit===dropin`（成员随容器迁移）后，对称（链全等）**或**非对称（`rm.length===dm.length+1 && rm[0]===exportName && dm.every((m,i)=>m===rm[i+1])`）皆抑制；binding 扫描器（`scanProjectMembers`）与实例扫描器（`instanceFinding`）两处调用点统一替换。binding 扫描器侧对称条目本就 dormant（接口静态访问 `binding.Stat.ino` 在值位非法，永不命中），实测净增来自实例侧 66 条（`Stat`/`Stream`/`Watcher`/`ResultSet` 成员）。对称条目改要求 `repl.kit===dropin` 0 回归（11 条原抑制条目 repl.kit 全等于 dropin）。净增 auto +66（3272→3338），manual 1441→1375，chain-length-mismatch 573→507（cross-kit 31 不变）。剩余 manual 大头经深挖确认为调用形状真变化、不可安全自动替换：cross-kit 31 为 1-seg 歧义 `bluetoothManager.on/off`（10）+ 类成员随容器跨 kit 但 repl.kit 解析为非索引 kit 路径（如 `@ohos.application.Configuration.language`→`@ohos.app.ability.Configuration.language`，待 parser/抑制修正）；chain-length-mismatch 507 多为枚举↔枚举成员等语义变化（如 `Event`→`Event.USER_LOGIN`、`AbilityLifecycleState.UNINITIALIZED`→`AbilityLifecycleState`）；no_replacement 831 无 @useinstead。**round ⑩（2026-08-11）**：**①冗余 kit-namespace 前缀剥离（B1）+ ②verifyInstanceSafe 非对称扩展（B2）+ ③classifier instanceSafe 保真度修正**。真实 SDK 的 `@useinstead` 用 `Class#method` 记法（`#` 分隔 path 与 leaf，如 `ohos.pasteboard.pasteboard#createData`、`ohos.arkui.UIContext.Router#getStackSize`），round ⑥ 已滤 `#event:NAME` 含冒号碎片，但 `#member`（无冒号）保留→repl.members 多一段。其中两子形状：**(B1) namespace-function 改名**——repl.members=[kitLastSeg, leaf]（2 段，kitLastSeg===repl.kit 末段且===dep.exportName），如 `pasteboard.createHtmlData`→`pasteboard#createData`（repl=[pasteboard,createData]）。此 repl[0] 是冗余 kit 命名空间前缀（repl.kit 已编码 kit），剥后 [createData] 成 same-kit 1-seg leaf 改名→`auto_rename_member`。新增 `normalizeRedundantNamespacePrefix(dep, repl)`：repl.members.length===2 && repl.members[0]===repl.kit 末段 && repl.members[0]===dep.exportName && dep.members.length===1 → 剥为 [repl.members[1]]。18 条非对称 1→2 namespace-function 改名归 auto。**(B2) instance-method 改名**——repl.members=[Container, newLeaf]（2 段，Container===dep.exportName 是真实类/接口，如 `Router.getLength`→`Router#getStackSize`，dep.exportName=Router、dep.members=[getLength]、repl=[Router,getStackSize]）。`verifyInstanceSafe` 旧门 `dep.members.length < 2` 只验对称（dep.members>=2，typeHead=dep.members[0]）；非对称被拒→instanceSafe 未打→实例扫描器对 `router.getLength()` 报 manual。扩展：门改为 `dep.members.length === 0`（仅拒空），非对称时 typeHead=dep.exportName；repl.members[0]===typeHead 且新 leaf 是同封闭类/接口的 sibling 成员→打 instanceSafe。12/19 非对称 1→2 instance-method 条经验证归 auto（7 条新 leaf 非 sibling 留 manual，如 `Lone.oldMethod`→`Lone#newMethod` 但 Lone 无 newMethod 成员）。instance scanner 对 `router.getLength()`→`router.getStackSize()` 走 rename-member 自动拼。**③classifier 保真度修正**：round ④起 instanceSafe 标已存在（对称 1-seg-repl，~46 条），但 classifier 脚本缺 instanceSafe 分支→误归 chain-length-mismatch。三脚本（classify-doc/classify/classify-full）加 `if (c===null && e.instanceSafe) c="auto";` 分支后正确计入。**净增 auto +77（3338→3415）**：+30 真新增 scanner 覆盖（B1 18 + B2 12）、+47 classifier 保真度修正（既有 instanceSafe 条目由 chain-mismatch 重计入 auto，非新覆盖）。manual 1375→1298，chain-length-mismatch 507→431（cross-kit 31、no_replacement 831 不变）。

**嵌套 namespace module-move bug（已修）**：indexer 原以 `isNamespaceLevel`（node 为 ModuleDeclaration）判 kit 级 move，但嵌套 namespace（如 bluetoothManager 内的 `namespace BLE`）也是 ModuleDeclaration，其成员的 cross-kit @useinstead 被错记为整 kit 的 newKit。修正：要求该 namespace 无外层 ModuleDeclaration 祖先（即文件最外层 kit 命名空间）才记 kitIndex module-move/manual。

**@useinstead 后缀解析**：解析器原只认 `ohos.<完整点分 kit>` 前缀。SDK 偶有简写/缺中段形式（`ohos.distributedDataObject.create` 实指 `@ohos.data.distributedDataObject`；裸 `distributedDataObject`）。新增 `resolveBySuffix`：当 head 是唯一已知 kit 点分名的末段（大小写不敏感）时解析为该 kit。`ohos.` 分支在 longestKitPrefix 失败时回退到后缀解析；裸标识符分支同样回退。unresolved 48→7。保守：多义不解析。

测试：`npm test`（node --test，159 个，全部通过）。SDK 路径见 [[harmonyos-sdk-location]]。

**实例方法检测（type-aware）**：两层。①regex 增量 1：`scanProjectInstanceMembers` 用正则从 import 解析变量类型（`ns.Type` 限定式 / `T` 命名导入，同时支持 .ts/.ets 无 tsc），匹配带类型注解的本地变量/参数作 receiver 的实例方法。indexer 用 **sibling check**（在 SDK AST 里查 repl leaf 是否为同接口/类成员）打 `instanceSafe`：支持两种 repl 形状——单段 `[newLeaf]`（如 getString→getStringValue）与**类型保留两段** `[Type,newLeaf]`（repl[0]===dep 类型，如 Window.show→Window.showWindow，仅 leaf 变）。已验证→rename-member 自动拼 `var.leaf→var.replLeaf`（真实 map 256 条，较增量1 的 46 条 +210，覆盖 window/rpc/account.appAccount/account.osAccount/audio 等）；未验证/跨 kit→manual finding 报 @useinstead 目标（不再静默漏报）。**容器 crossKitDropin 抑制**：实例成员若其容器（exportName）经 crossKitDropin 跨 kit 同名迁移、且 repl.kit===dropin 目标、链全等，则 `instanceFinding` 经 `ContainerDropinResolver` 返回 null 抑制（容器 import 已被 rewrite-import 改写覆盖，实例访问在重指后的 binding 上自然解析，不再冗余报 manual）。②tsc 增量 2：`scanProjectInstanceMembersTsc` 用 ts-morph 对项目 **.ts** 文件（.ets 排除，struct/build() tsc 解析不了）做类型检查，解析无类型注解的运行时 receiver（`const ctx = featureAbility.getContext()` 同步、`const win = await window.getLastWindow()` 经 await 解开 Promise）。kit 归属用 indexer 新持久化的 **fileKit**（文件→kit 映射，API 24 共 695 条，与 dep 条目同源归属保证 key 一致），辅以 `declare module` 回退（合成 fixture）。复用 instance index + instanceSafe 分类。SDK 缺失或无 .ts 文件→静默降级（regex scanner 仍为基准）。真实 SDK 实测：ctx.setShowOnLockScreen/win.show→manual 检测、rm.getString→rm.getStringValue 自动（皆由静默漏报转为检出）。仍漏：.ets 中的无注解 receiver、多段 repl 路径在实例上、跨 kit FA→stageless 自动改写（receiver 合成）。

**fileKit 映射**：`DeprecationMap.fileKit: Record<string,string>`，key 为 SDK api 树内相对路径（正斜杠），value 为 indexer re-export 追溯出的归属 kit（孤儿文件为 `@?`）。tsc scanner 据已解析类型的声明文件回查 kit。另需 `paths` 映射（`@ohos.*`/`@system.*`→`./@ohos.*.d.ts`）+ baseUrl= sdkApiDir，使默认导入解析真实 SDK 的 module 式声明文件（`declare namespace X` + `export default X`，非 `declare module`）。

测试：`npm test`（node --test，159 个，全部通过）。SDK 路径见 [[harmonyos-sdk-location]]。
